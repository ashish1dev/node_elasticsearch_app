# Intoduction to Elasticsearch with node.js

An app showcasing how to make elasticsearch http calls from a nodejs server.

Before starting the app, run :

```
npm install
```

Just start the app using the following command:

```
npm start
``` 
